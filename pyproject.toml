[project]
name = "adl-capability-matrix"
version = "0.1.0"
description = "Claim-capped cluster matrix and compatible-build queue for beyond-repair repos."
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
